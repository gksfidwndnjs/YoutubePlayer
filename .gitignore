node_modules/
.claude/
*.log
dist/
build/
# but keep the NSIS installer customization (a source file, needed to build)
!build/installer.nsh
docs/

# Local Google OAuth credentials (bundled into builds, never in the repo)
src/google-config.js

# Cached YouTube player scripts
1778*.js
