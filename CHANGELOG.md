# 변경 기록

## 1.4.12 (2026-08-15)

- **재생목록 팝업이 빈 화면으로 뜨던 문제 수정.** 1.4.11에서 경고 아이콘 판정 함수의
  이름을 바꾸면서 갱신 버튼을 그리는 쪽의 호출부가 옛 이름으로 남았다. 팝업을 열면
  거기서 예외가 나 목록을 그리는 단계까지 가지 못했고, 검색창에 무언가를 입력해야
  목록이 나타났다(입력 처리만 목록을 따로 다시 그린다).

## 1.4.11 (2026-08-15)

- **일부 곡이 재생되지 않고 건너뛰어지던 문제 수정.** yt-dlp가 기본으로 고르는 재생
  클라이언트의 주소를 YouTube가 영상에 따라 거부해, 추출은 성공해도 실제 요청이 전부
  403으로 막혔다. 재생 클라이언트를 순서대로 시도하면서 1바이트를 실제로 받아보고
  통과한 것만 쓴다. 영상별로 성공한 클라이언트를 기억해 다음부터는 바로 사용한다.
  - 재생 도중 주소가 만료돼 끊기면 새 주소를 받아 끊긴 지점부터 이어 재생한다.
  - 같은 곡에 대한 동시 요청은 한 번으로 합쳐 첫 재생이 빨라졌다.
- **공식 앨범을 검색으로 추가한다.** YouTube Data API는 자동 생성된 공식 앨범
  재생목록을 색인하지 않아 아티스트나 앨범 이름으로는 찾을 수 없었다. YouTube Music
  쪽에서 앨범을 함께 조회해 검색 결과에 **공식 앨범** 항목으로 보여준다.
- 검색창에 재생목록 URL뿐 아니라 재생목록 ID를 그대로 넣어도 인식한다.
- 추천·공식 음원 아래의 화살표 버튼으로 결과를 더 불러온다.
- 가사는 디스크에 캐시해 다시 조회하지 않는다(찾은 가사 30일, 없는 곡 7일).
- 재생목록의 ⚠ 표시 기준을 "원본을 잃어버린 목록"으로 좁혔다. 아직 열지 않은 Google
  재생목록에까지 경고가 붙던 문제가 없어졌다.

## 1.4.10 (2026-08-14)

- **커버 아트가 없는 폴더 재생목록을 열면 앱이 멈추던 문제 수정.** 1.4.9는 목록을 그릴 때
  모든 트랙의 앨범 아트를 한꺼번에 요청해, 351곡 폴더에서 ffmpeg 프로세스 351개가 동시에
  떴다. 게임 OST처럼 아트가 하나도 없는 폴더에서는 전부 실패하고 기록도 메모리에만 남아
  앱을 켤 때마다 같은 일이 반복됐다.
  - 앨범 아트는 화면에 실제로 보이는 줄만 꺼낸다(재생목록을 접어두면 아예 실행되지 않는다).
  - 동시에 최대 2개까지만 추출한다.
  - "아트 없음" 판정을 디스크에 기록해 다음 실행부터는 다시 시도하지 않는다. 파일이
    바뀌면(수정 시각·크기 기준) 다시 확인한다.
  - 같은 파일에 대한 중복 요청은 한 번으로 합친다.

  자세한 조사 기록은 [docs/folder-playlist-cover-art-storm.md](docs/folder-playlist-cover-art-storm.md) 참고.

## 1.4.9 (2026-08-12)

- **내 PC의 폴더를 재생목록으로 추가한다.** 재생목록 팝업의 `＋ 폴더` 버튼으로 폴더를
  고르면 하위 4단계까지의 음악 파일(m4a·mp3·flac·opus·wav 등)을 자연 정렬 순서로 담은
  재생목록이 만들어진다. 인터넷·API 키·로그인 없이 파일을 그대로 재생한다.
- 곡 정보는 파일명에서 읽는다. `Title [videoId]`(이 앱이 받은 파일)은 영상 ID까지 복원해
  YouTube 썸네일이 붙고, `07 IU - 밤편지`·`1-18. Artist - Title` 형태는 트랙 번호를 떼고
  아티스트와 제목으로 나뉜다. 이 정보로 가사 검색도 그대로 동작한다.
- 앨범 아트는 재생·목록 표시 시점에 ffmpeg로 파일에서 꺼내 캐시한다. 아트가 없는 파일은
  기본 이미지로 두고 다시 시도하지 않는다.
- 폴더 재생목록은 열 때와 앱 실행 시 다시 훑는다. 폴더에 넣은 파일은 자동으로 추가되고
  지운 파일은 빠진다(디스크 순서가 그대로 재생 순서).
- 폴더 재생목록에는 다운로드 버튼과 일괄 다운로드 줄이 나오지 않는다. 목록에서 제거해도
  폴더와 파일은 지워지지 않으며, 삭제 확인창이 이를 알린다.
- 파일 경로를 `pathToFileURL`로 변환한다. 파일명에 `#`나 `?`가 있으면 URL이 잘려
  재생에 실패하던 문제도 함께 해결된다.

## 1.4.8 (2026-08-11)

- 다운로드 실패 시 2.5초 → 5초 간격으로 최대 3회 자동 재시도. 일괄 다운로드가 연달아
  요청을 보내 YouTube에서 일시적으로 받던 `HTTP Error 403`을 대부분 흡수한다.
- 삭제·비공개·연령 제한·지역 차단처럼 재시도해도 소용없는 실패는 즉시 건너뛴다.
- 일괄 다운로드 곡 사이에 간격을 둬 차단을 덜 유발한다.
- 앱 실행 시 yt-dlp를 자동으로 최신 버전으로 갱신한다. 관리자 권한이 필요 없고,
  실패해도 앱 사용에는 지장이 없으며, 실제로 갱신됐을 때만 알린다.
- 번들 yt-dlp를 `2026.03.17` → `2026.07.04`로 갱신.

## 1.4.7 (2026-08-11)

- **설치본에서 다운로드가 실패하던 문제 수정.** `youtube-dl-exec`이 실행 파일 경로에
  공백이 있으면 셸 모드로 전환하며 인자를 따옴표로 감싸지 않아, 설치 경로
  `…\Metalwave for YouTube\…` 에서 모든 인자가 공백에서 쪼개졌다. 그 결과 저장 파일명이
  영상 ID와 확장자를 잃어 앱이 인식하지 못했고(→ 같은 곡 반복 다운로드), 썸네일 삽입도
  매번 실패했다. 이제 셸 없이 직접 실행한다. 자세한 조사 기록은
  [docs/download-failure-spaced-path.md](docs/download-failure-spaced-path.md) 참고.
- 재생목록 팝업에 검색창 추가. 갱신 버튼과 검색창은 고정되고 목록만 스크롤된다.
  `Enter`는 결과가 하나일 때 이동, `Esc`는 검색어를 지우거나 팝업을 닫는다.

## 1.4.6 (2026-08-11)

- 다운로드 실패 시 yt-dlp가 알려준 **실제 원인**을 표시한다. 전체 오류는
  `userData/download-errors.log`에 기록되며 설정 → Downloads → 로그 열기로 확인할 수 있다.
  오류 알림은 9초간 유지되고 창 안에서 줄바꿈된다.
- 음악 폴더를 변경하면 기존에 받아둔 곡을 새 폴더로 옮길지 물어본다. 재생목록별 하위
  폴더 구조를 유지하고, 대상에 같은 파일이 있으면 덮어쓰지 않으며, 다른 드라이브로도
  옮길 수 있다(복사 후 원본 정리). 이동 중에는 진행률이 표시된다.

## 1.4.5 (2026-08-11)

- 업데이트 다운로드 진행률을 플레이어 위 진행률 바와 작업 표시줄에 표시한다.
- Google 로그인이 만료되면(`invalid_grant`) 죽은 토큰을 지우고 재로그인이 필요하다고
  한 번만 알린다. 이전에는 재생목록 갱신이 조용히 실패했다.
- 재생목록 팝업에 전체 갱신 버튼 추가. 원본 링크가 없어 갱신할 수 없는 목록은 ⚠로 표시.
- 창 크기 변경 시 도킹된 모서리를 유지한다.

## 1.4.4 (2026-08-11)

- 앱 실행 시 연결된 재생목록을 자동으로 갱신한다. 원본에서 삭제된 곡은 빠지고 새 곡은
  추가되며, 기존 순서와 직접 추가한 곡은 유지된다.
- URL로 추가한 재생목록이 원본 재생목록 ID를 저장한다(이전에 추가한 목록은 다시 추가해야
  갱신 대상이 된다).
- 로컬 파일 인덱스가 `m4a` 외 컨테이너(webm/opus 등)도 인식하고 하위 폴더를 함께 훑는다.
  이전에는 이런 파일이 영구히 "미다운로드"로 잡혀 매번 다시 받았다.
- 다운로드 요청 시 디스크를 먼저 확인해 이미 받은 곡을 다시 받지 않는다.
- 음악 폴더가 OneDrive의 클라우드 전용 파일이면 한 번 경고한다. 이 경우 재생할 때마다
  OneDrive가 파일을 다시 내려받아 Windows가 다운로드 알림을 띄운다.

---

이전 버전의 변경 내역은 [GitHub 릴리스](https://github.com/gksfidwndnjs/YoutubePlayer/releases) 참고.
